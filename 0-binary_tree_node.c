#include "binary_trees.h"
/**
 *
 *
**/

binary_tree_t *binary_tree_node(binary_tree_t *parent, int value)
{
	value = malloc(sizeof(binary_tree_t));
	if (value != NULL)
	{
		//value->parent = NULL;//
		value->n = n;
	}
	return (value);
}
